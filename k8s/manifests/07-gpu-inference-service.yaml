apiVersion: v1
kind: Service
metadata:
  labels:
    app: gameforge
  name: gpu-inference
  namespace: gameforge
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  selector:
    app: gameforge
    component: gpu-inference
  type: ClusterIP
